/**
 * core/config.js — configuración del frontend.
 * El backend (Pages Functions) vive en el mismo origen → rutas relativas, sin CORS.
 */
export const CONFIG = {
  API_BASE: "/api",
  TIENDA_NOMBRE: "Quien no corre, vuela",
  MONEDA: "EUR",
};
