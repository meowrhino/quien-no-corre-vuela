{
  "name": "quien-no-corre-vuela",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler pages dev . --compatibility-flags=nodejs_compat",
    "deploy": "wrangler pages deploy .",
    "db:init": "wrangler d1 execute shop --remote --file=./data/schema.sql",
    "db:init:local": "wrangler d1 execute shop --local --file=./data/schema.sql"
  },
  "dependencies": {
    "hono": "^4.6.14",
    "stripe": "^18.3.0"
  },
  "devDependencies": {
    "wrangler": "^3.95.0"
  }
}
